# Expo's Metro bundler expects a flat (hoisted) node_modules layout. pnpm's
# default isolated/symlinked layout causes duplicate React copies and breaks
# react-native package "exports" resolution. node-linker=hoisted flattens the
# install so the whole monorepo shares one react / react-native.
node-linker=hoisted
